diff --git a/src/game/Maps/Map.cpp b/src/game/Maps/Map.cpp index fe7fdbc7..e9becd6f 100644 --- a/src/game/Maps/Map.cpp +++ b/src/game/Maps/Map.cpp @@ -822,9 +822,9 @@ inline void Map::UpdateCells(uint32 map_diff)      else          UpdateActiveCellsSynch(now, diff);   -    if (IsContinent() && !unitsMvtUpdate.empty()) +    if (!unitsMvtUpdate.empty())      { -        if (m_motionThreads->status() == ThreadPool::Status::READY) +        if (IsContinent() && m_motionThreads->status() == ThreadPool::Status::READY)          {              for (auto it = unitsMvtUpdate.begin(); it != unitsMvtUpdate.end(); it++)                  m_motionThreads << [it,diff](){ @@ -835,8 +835,8 @@ inline void Map::UpdateCells(uint32 map_diff)          }          else          { -            // Eluna disables parallel object updates, but units still reach this -            // queue because the configured motion-thread count remains nonzero. +            // Eluna disables parallel map updates because Lua states are not thread-safe. +            // Keep movement updates on the map thread rather than discarding the queue.              for (Unit* unit : unitsMvtUpdate)                  if (unit->IsInWorld())                      unit->GetMotionMaster()->UpdateMotionAsync(diff);